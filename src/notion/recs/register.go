package recs

// Register's a new update iteration, which is basically just a text
// insertion at a given position in a given note by a given user.
//
func Register(text string, at int, userId string, noteId string, outgoing chan map[string]interface{}) {

}
